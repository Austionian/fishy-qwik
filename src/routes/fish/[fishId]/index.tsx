import { component$ } from "@builder.io/qwik";
import { RequestHandler } from "@builder.io/qwik-city";
import { Fish } from "..";

export const onGet: RequestHandler<Fish> = async (ctx) => {
        return 
    }

export default component$(() => {
  return <div>Todo!</div>;
});
